Hello ULAB .
Bangadesh  Good bye.
Nothing to do.
cat filename.extension 
-> git checkout master
-> git log filename.extension
-> git checkout hash filename.extension
-> cat filename 
-> git checkout master -f

->git push origin master
->git pull origin master
->git commit -am "write something about what you did"
->git stash
->git stash pop/apply
->git stash list
->git stash pop stash@{1}
->git stash clean
->git remote add origin /ur/
->git reset --soft /hash/
->git reset --hard /hash/
->git clone /url/
->git rm /filename.extention/
->git mv /filename.extention/ /filename.extention/
->git branch /branch_name/
->git chechout /branch_name/
->git branch -d hello_england
->git merge /branch name/
git push o
